interface CardProps {
    source?: string;
    header?: string;
    description?: string;
}

type Card = {
    id: string | number;
    source?: string;
    header?: string;
    description?: string;
}